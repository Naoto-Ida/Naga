import type {
	MoveCategory,
	MoveElement,
	MoveTargetGroup,
	MoveType,
} from './types';

export const DEFAULT_CATEGORY: MoveCategory = 'physical';
export const DEFAULT_ELEMENT: MoveElement = 'neutral';
export const DEFAULT_TYPE: MoveType = 'offensive';
export const DEFAULT_TARGET_GROUP: MoveTargetGroup = 'single';
